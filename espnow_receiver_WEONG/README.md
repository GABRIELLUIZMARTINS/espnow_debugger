# Log record
To generate logs.

### To flash: ### 
Enter the command:
  get_idf && idf.py -p [PORT] flash

#### To monitor: ####

To generate log, run:
  get_idf && idf.py -p [PORT] monitor | tee debug/data.log



